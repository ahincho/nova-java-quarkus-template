# Generated by nova-java-quarkus-template — DO NOT EDIT
# after the rename task runs. Re-running `gradle rename` with different
# values will overwrite this file with the new coordinates.
group = __GROUP__
artifactId = __ARTIFACT__
version = 0.1.0-SNAPSHOT

# Java toolchain (matches Nova Platform: Java 25).
org.gradle.java.installations.auto-download = true
org.gradle.java.installations.auto-detect = true

# Quarkus platform coordinates — aligned with nova-quarkus-parent (Maven)
# so a Gradle-only consumer gets the same Quarkus version + extension BOM
# as the Maven archetype path.
quarkusPlatformGroupId = io.quarkus.platform
quarkusPlatformArtifactId = quarkus-bom
quarkusPlatformVersion = 3.33.2.1

# Nova Platform BOM coordinates.
novaNotificationsQuarkusExtensionVersion = 1.1.6