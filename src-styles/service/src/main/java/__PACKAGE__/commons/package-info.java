/**
 * Cross-cutting common utilities shared across features.
 *
 * <p>Reusable, dependency-free helpers with no business meaning:
 * constants, small utility types, generic result/error wrappers and
 * other technical building blocks used by more than one feature. Nothing
 * here may depend on a feature's domain — if it needs domain knowledge,
 * it belongs in that feature, not in commons.
 */
package __PACKAGE__.commons;